
//Este archivo es el que crea el context, suele ser el primero que se crea por su sencillez
import { createContext} from 'react'

//creamos el context
const proyectoContext = createContext();

export default proyectoContext;

 